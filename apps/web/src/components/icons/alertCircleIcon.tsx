import { AlertCircleIcon as LucideAlertCircleIcon } from "lucide-react";

import type { IconProps } from "@/components/icons/iconTypes";

export function AlertCircleIcon(props: IconProps) {
  return <LucideAlertCircleIcon {...props} />;
}
