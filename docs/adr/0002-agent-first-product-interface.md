# Agent-first product interface

Church Task will treat agent access as a first-class product interface alongside the web UI. We will build MCP and CLI surfaces using Effect so tools like Claude Code and Codex can perform most church planning and task-management workflows through prompts; agent and CLI surfaces may expose advanced power-user operations beyond the UI, while every core capability remains available through the UI.
